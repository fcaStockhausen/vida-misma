# 2026-06-02

Maybe the simulation itself lacks complexity,
maybe its the agents that dont have a purporse.

Also i note that there is an unused input
and the output.

Lets change some quick stuff. First WTC shouldnt propose unbuilt machines, all machines should be constructed from the ground up, by agents, all machines must be placed in tiles. For example, food machine should be in foodResource tiles, a renewable resource.

Scrap is not renewable, but you can make more scrap in the output machine, the machine that can make  quota also makes scrap so agents can make more machines.

Food should be plenty, but should be processed so it can be done.

We need to develop a system of priorities, every agent should understand that they should cooperate or they dead.


- When everyone dies simulation should stop; then we could have the results and the stories, the things that happened on this world

- People will try to survive but we wont make the system make them survive freely

- Quota completion should heal the factory also

- Agents should know how the supply chain works


Is it that we are not designing the utility functions correctly? all utility functions should be linear? is it this way that its implemented in other situations? in literature?


## Scrap material
    Only to built machines, if there are enough food machines, priority should be output machines,
    food machines maybe need to be more efficient



## Wave function collapse
    Should never have constructed stuff in it, except the initial two storages and the outputs in the outer walls, conveyors should be placed  